define(['app', 'backbone'],
    function (appInstance, Backbone) {

        describe('The Application', function () {

            it('should have tests', function () {
                expect(appInstance).to.exist;
            });

        });

    }
);